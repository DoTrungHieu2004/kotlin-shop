package com.hieu10.kotlinshop.ui.theme

import androidx.compose.ui.unit.dp

object AppElevation {
    // Standard flat surfaces
    val Level0 = 0.dp

    // Subtle lift for product cards
    val Card = 2.dp

    // Top app bars and bottom navigation
    val AppBar = 4.dp

    // Floating action buttons (chatbot)
    val FAB = 6.dp

    // Models and popups
    val Dialog = 8.dp
}